<<<<<<< HEAD
# ---- Database (PostgreSQL) ----
DATABASE_URL=postgresql+psycopg2://sharedlinks_user:sharedlinks_pass@localhost:5432/sharedlinks_db
SQL_ECHO=false

# ---- CORS ----
# The client is a Vite app (npm run dev -> http://localhost:5173 by default)
CORS_ORIGINS=http://localhost:5173

# ---- Shared link URLs ----
PUBLIC_BASE_URL=https://localhost/share

# ---- File storage (My Files module) ----
# Local disk today; swap src/files/storage.py's backend to migrate to S3/Azure.
FILES_STORAGE_DIR=./storage/files
MAX_UPLOAD_SIZE_MB=200
# Comma-separated, no dots, lowercase. Leave unset to use the built-in default list.
# ALLOWED_FILE_EXTENSIONS=pdf,doc,docx,xls,xlsx,ppt,pptx,txt,csv,md,jpg,jpeg,png,gif,zip

# Real AES-256-GCM at-rest encryption key (urlsafe-base64, must decode to 32 bytes).
# Generate one with:
#   python -c "import os, base64; print(base64.urlsafe_b64encode(os.urandom(32)).decode())"
# REQUIRED before any file upload will work - there is no insecure default.
FILES_ENCRYPTION_KEY=

# Default per-user storage quota, applied when a user row is created.
DEFAULT_STORAGE_QUOTA_GB=500

# How long a file stays in Trash before it's permanently purged.
TRASH_RETENTION_DAYS=30
TRASH_PURGE_INTERVAL_HOURS=24

# ---- Email (FastAPI-Mail) ----
MAIL_ENABLED=false
MAIL_USERNAME=your_email@example.com
MAIL_PASSWORD=your_app_password
MAIL_FROM=no-reply@trustshare.example.com
MAIL_FROM_NAME=TrustShare
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_STARTTLS=true
MAIL_SSL_TLS=false

# ---- Background scheduler ----
SCHEDULER_ENABLED=true
EXPIRATION_CHECK_INTERVAL_MINUTES=60
EXPIRY_WARNING_HOURS=24

LOG_LEVEL=INFO

=======
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/authdb
JWT_SECRET=change-this-to-a-long-random-string
ACCESS_TOKEN_MINUTES=30
REFRESH_TOKEN_DAYS=7
MFA_TOKEN_MINUTES=5
EMAIL_OTP_MINUTES=10

# OAuth: powers both "Connect Google/Microsoft" (Security page) and
# "Continue with Google/Microsoft" (Sign In / Sign Up) buttons.
# Google: create credentials at https://console.cloud.google.com/apis/credentials
# Microsoft: register an app at https://portal.azure.com > App registrations
# Add BOTH redirect URIs below to each provider's app configuration.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_LINK_REDIRECT_URI=http://localhost:5173/oauth/google/callback
GOOGLE_LOGIN_REDIRECT_URI=http://localhost:5173/oauth/google/login-callback

MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
MICROSOFT_LINK_REDIRECT_URI=http://localhost:5173/oauth/microsoft/callback
MICROSOFT_LOGIN_REDIRECT_URI=http://localhost:5173/oauth/microsoft/login-callback

# Real email sending for password reset & MFA email codes.
# Leave blank to keep using the "dev code" fallback shown in the UI.
# Example for Gmail: host=smtp.gmail.com port=587, user=your address,
# password=an App Password (not your regular password).
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
FRONTEND_URL=http://localhost:5173
>>>>>>> 8590378 (Add user authentication module and database schema)
