import React from 'react';
import './Encryption.css';

export default function Encryption() {
  return (
    <div style={{ padding: '2rem', color: 'var(--text-primary)' }}>
      <h1 style={{ fontSize: '1.75rem', fontWeight: 800, marginBottom: '0.5rem' }}>Encryption</h1>
      <p style={{ color: 'var(--text-muted)' }}>End-to-end encryption management — coming soon.</p>
    </div>
  );
}
