import React from 'react';

export default function Dashboard() {
  return (
    <div style={{ padding: '2rem', color: 'var(--text-primary, #f8fafc)' }}>
      <h1 style={{ fontSize: '1.75rem', fontWeight: 800, marginBottom: '0.5rem' }}>Dashboard</h1>
      <p style={{ color: 'var(--text-muted, #94a3b8)' }}>Welcome to SecureShare.</p>
    </div>
  );
}
